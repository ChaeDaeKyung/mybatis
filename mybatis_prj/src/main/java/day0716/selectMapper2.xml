<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="day0716">
	
	<!-- 컬럼하나 여러행 조회
		 부서번호를 입력받아 해당부서의 모든 사원번호 검색
		 List<Integer> 사원버호 리스트 = ss.selectList("아이디",부서번호); -->
	<select id="scmr" parameterType="int" resultType="int">
		SELECT	EMPNO
		FROM	EMP 
		WHERE	deptno = #{ deptno }
	</select>
	<!-- 컬럼여러개 여러행 조회 -->
	<!-- 작은 값 조회 -->
	<!-- 큰 값 조회 -->
	<!-- like 조회 -->
	<!-- subquery -->
	<!-- union-->
	<!-- subquery &amp; join-->
	<!-- 테이블명이 동적일때-->
	<!-- 동적 쿼리 if-->
	<!-- 동적 쿼리 choose-->
	<!-- 동적 쿼리 set-->
	<!-- 동적 쿼리 each-->
</mapper>