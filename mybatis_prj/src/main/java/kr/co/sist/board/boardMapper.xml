<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.board">
	<!-- 총 게시글의 수 -->
	<select id="totalCount" parameterType="rangeDTO" resultType="int">
		select 	count(*) cnt
		from	board
		<where>
			<if test="keyword neq null and !keyword neq ''">
				instr( ${ field }, #{ keyword }) != 0
			</if>
		</where>
	</select>
	<!-- 시작번호와 끝 번호의 게시글 -->
	<select id="selectBoard" parameterType="rangeDTO" resultType="boardDomain2">
		select num, id, title, input_date, cnt, upfile
		from (select NUM, ID, TITLE, INPUT_DATE, CNT,upfile, row_number() over(order by input_date desc ) rnum
			  from board
			<where>
			<if test="keyword neq null and !keyword neq ''">
			  instr(${ field }, #{ keyword } ) != 0 
			</if>
			</where>
		 	  ) where rnum between #{ startNum } and #{ endNum }
	</select>
	<select id="selectBoardDetail" parameterType="int" resultType="boardDomain2">
		select id, title, content,ip,input_date, cnt
		from board
		where num = #{ num }
	</select>
	<!-- 게시글을 읽을때마다 뷰 카운트 1증가 -->
	<update id="updateCnt" parameterType="int">
		update board
		set	cnt = cnt + 1
		where num = #{ num }
	</update>
	<insert id="insertBoard" parameterType="boardDTO">
		insert into board( num, id, title, content, ip
		<if test="upfile neq null">
		, upfile
		</if>
		) values ( board_seq,#{ id },#{ title },#{ content },#{ ip }
		<if test="upfile neq null">
		,#{ upfile }
		</if>
		)
	</insert>
	<update id="updateBoard" parameterType="boardDTO">
		update board
		set title = #{ title }, content = #{ content }, ip = #{ ip } 	
		where num = #{ num } and id = #{ id }
	</update>
	<delete id="deleteBoard" parameterType="boardDTO">
		delete from board
		where num = #{ num } and id = #{ id } 
	</delete>
</mapper>